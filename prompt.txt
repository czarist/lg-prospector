# Comandos separados:
#   prompt-prospeccao.txt  — cockpit (os três + painel) ou hunts/mailman isolados
#   prompt-correcao.txt    — purge nicho/nacionalidade, qualidade, bounces

# painel único:
./cockpit
./cockpit --attach   # só o painel de novo
./cockpit --stop     # derruba os três

tmux new-session -s hunt \; \
  send-keys 'tail -f /home/lucas/lg-prospector/logs/hunt/console_loop.log' C-m \; \
  split-window -v \; \
  send-keys 'tail -f /home/lucas/lg-prospector/logs/hunt/console_generalista.log' C-m \; \
  split-window -v \; \
  send-keys 'tail -f /home/lucas/lg-prospector/logs/mailman/console.log' C-m \; \
  select-layout even-vertical


  